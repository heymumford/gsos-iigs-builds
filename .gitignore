# Build artifacts
*.o
*.OBJ
*.obj
*.a
*.lib
*.bin
*.bin.gz
*.dsk
*.po
*.2mg
*.rom

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*~

# Source code mounted out-of-band — never commit Apple IP
sources/
includes/
libraries/
GS_OS_Source/

# Local environment overrides
.env
.env.local
phases/local-config.sh

# User-supplied ROM and disk images (never commit Apple IP)
roms/
disk-images/

# Working-tree-only handoff state — never commit
.claude/

# Fitness time-series log (running log, too noisy to commit; snapshot is committed)
data/fitness-history.ndjson
