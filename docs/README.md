# Docs

Holds porting notes and reference material that supports the build but is not itself executable.

Planned contents:

- `mpw-to-apw-make.md` — translating MPW IIGS Makefiles to APW Make syntax
- `partition-layout.md` — the multi-32MB ProDOS partition recipe (source / objects / tools)
- `emulator-setup.md` — bringing up KEGS or GSplus with a ProDOS volume large enough to host APW + sources
- `hardware-vs-emulator.md` — what Phase 1 fitness must check differently in each environment
